services:
  ingest:
    build:
      context: ../../..
      dockerfile: rag/apps/ingest/Dockerfile
    ports:
      - "4000:4000"
    environment:
      - PORT=4000
      - API_URL=${API_URL}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    restart: unless-stopped
